# db config
#spring.datasource.url=${dbLocal_url}
#spring.datasource.username=${dbLocal_user}
#spring.datasource.password=${dbLocal_password}

spring.datasource.url==jdbc:mysql://localhost:3306/IoT
spring.datasource.username=root
spring.datasource.password=

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true
spring.jpa.database=mysql
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.mvc.pathmatch.matching-strategy = ANT_PATH_MATCHER

# jwt
jwt.secret = 6P7Q8SATBUDWEXFZH2J3M5N6P7R9SATCVDWEYGZH2K4M5N7Q8R9TBUCVEX
jwt.expiration = 43200
spring.output.ansi.enabled=always

#Avoid error trace sending on exception
server.error.include-stacktrace=never

# mqtt config
mqtt.automaticReconnect=true
mqtt.cleanSession=true
mqtt.connectionTimeout=10
mqtt.clientId=${mqttClient}
mqtt.hostname=${mqttHost}
mqtt.port=${mqttPort}
mqtt.username=${mqttUser}
mqtt.password=${mqttPass}

# active environment
#spring.profiles.active=local
